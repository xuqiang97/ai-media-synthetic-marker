[project]
name = "ai-media-synthetic-marker"
version = "1.0.0"
description = "Windows GUI for writing and strictly verifying contains-synthetic-performer in XMP dc:subject."
readme = "README.md"
requires-python = "==3.14.6"
license = { file = "LICENSE" }
authors = [
    { name = "AI Media Synthetic Marker contributors" }
]
dependencies = []

[project.optional-dependencies]
build = [
    "pyinstaller==6.21.0"
]

[project.urls]
ExifTool = "https://exiftool.org/"
